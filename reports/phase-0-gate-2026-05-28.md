# Phase 0 Gate Report — generated 2026-05-28

## 1. Bugs found and resolved

59 of 61 Phase 0 rows pass (96.7%). The threshold for a WARNING is <80%, which this phase clears comfortably. No formal bug-plan IDs were filed against Phase 0 rows — the phase operated in build-from-scratch mode, not fix-bugs mode. Defects were caught and corrected within the same session before row close.

Documented friction points that were corrected in-session (not filed as bugs but constituting re-work):

- phase0-step-022 (invoke-agent.ts): bug-134 ambiguity from Phase 2 — two competing versions of the file existed. Resolved by canonical-version selection (resolveAuditBaseRef import wins over readModelConfig import). 0 retries needed; corrected at first close attempt.
- phase0-step-027 (/new-project smoke): 3 warnings surfaced mid-gate: missing assets/README.md, missing factory templates (turbo.json / tsconfig.json / package.json), and MCP per-agent sync behavior. All 3 addressed before flipping passes:true. The template gap (warning 2) required authoring 3 net-new files not in the original row plan — a mid-session scope expansion.
- phase0-step-042: initial 2026-05-27 attempt was marked NEEDS_WORK (no evidence file); re-attempted 2026-05-28 successfully. This is the only row with attempt_count evidence of a retry within Phase 0.

Two rows remain passes:false:

- phase0-step-016 (human-inspection): harness baseline smoke test — 5 sub-checks for the operator to walk through. Pending operator action. Not a blocker on code quality; a blocker on human sign-off.
- phase0-step-048 (human-inspection): Phase 0 closure HUMAN gate — this report is the artifact that enables it. The gate is the point of this report.

Mean time-to-fix: not measurable in minutes — Phase 0 is a greenfield build, not a bug-fix loop. The retry ladder and mean-time-to-fix metric is a Mode B / Phase 3 construct. DEFERRED — no target was set for Phase 0.

Top root cause of friction: missing factory scaffolds that Phase 2 assumed existed (templates, assets/README.md). Pattern appeared in 2 of 3 in-session corrections. NEEDS_WORK: this class should seed a Phase 1 pre-flight checklist.

## 2. Optimizations made (finished -> polished)

No rows are polished:true. Per the phase inputs, this is expected — polish is on-demand post-row, not gated by phase close. The polish-pass skill (phase0-step-015) is in place and ready for operator use in Phase 1.

No performance benchmarks were run at phase scope. The cost-projection module (phase0-step-055) and hook-regression suite (phase0-step-050) are tooling for future measurement, not optimizations of shipped phase0 work.

No optimization entries to report this phase. OK (by design).

## 3. Lessons learned (from LESSONS.md grep)

LESSONS.md currently contains only the template header and the append-point marker. The `/capture-lesson` skill (phase0-step-013) was authored and shipped, but no `phase0-` tagged lessons were appended during Phase 0 execution. The file has 20 lines total; zero lesson entries.

Finding: lesson capture was not executed during Phase 0 despite the skill being available from row 015 onward. The in-session corrections documented in PROGRESS.md (bug-134 ambiguity, /new-project template gap, row-042 retry) are strong candidates for lesson entries that were never captured.

No recurring patterns to report from LESSONS.md because the file is empty. The patterns visible from PROGRESS.md + evidence files are enumerated in Section 7 instead.

PROMOTE: the gap itself is a lesson. Recommend adding the following to LESSONS.md before Phase 1 start (operator action, not this report's artifact):

- Lesson: bulk-port rows where Phase 2 had competing or partial files require explicit canonical-selection logic before the port (see bug-134 / phase0-step-022). The "pick the one with more complete imports" heuristic should be codified.
- Lesson: /new-project runs reveal missing factory scaffolds that didn't exist yet (templates, assets/README.md). Run /new-project smoke early in future phases, not at row 027 of 61.

No entries qualify for the "recurring (xN)" promotion threshold because the lesson log is empty. NEEDS_WORK: `/capture-lesson` should be exercised at least once per phase; zero entries is a process gap.

## 4. Skill / rule promotions

No promotions were formally triggered this phase (no LESSONS.md entries hit the >=3 threshold; no ADR explicitly requests enforcement of a new skill). The following were authored as new skills and are already in the factory — they are not promotions, they are the deliverables:

New skills authored in Phase 0 (5 harness-new, per evidence/phase0-step-013-result.txt and evidence/phase0-step-015-result.txt):

- `.claude/skills/capture-lesson/SKILL.md` — lesson append workflow
- `.claude/skills/consult-lessons/SKILL.md` — lesson lookup before new work
- `.claude/skills/phase-gate/SKILL.md` — phase boundary retro invoker
- `.claude/skills/polish-pass/SKILL.md` — polished:false → true flipper
- `.claude/skills/sync-phase-plan/SKILL.md` — rebuild manifest sync (THE key gap not used this phase)

3 net-new agents authored in phase0-step-042:

- `.claude/agents/html-verifier.md` — Haiku, 6-point HTML contract checker
- `.claude/agents/lessons-agent.md` — Sonnet, 3-scope lesson capture
- `.claude/agents/agent-expert.md` — Opus, meta-agent self-improvement with operator-approval PROMOTION PROPOSAL gate

CLAUDE.md amendment proposed (to record the required-env rule already documented in §Model configuration):

- Rule already present: "Required env for orchestrator runs: ANTHROPIC_API_KEY set + ENABLE_PROMPT_CACHING_1H=1 exported." OK — no amendment needed.

No rule promotions to CLAUDE.md this phase beyond what shipped with Phase 0's initial authoring.

## 4b. Plan <-> code parity audit (REBUILD GUARANTEE)

Sampled 5 rows at random from the 59 passes:true Phase 0 rows. Rows chosen to span categories and section groups: phase0-step-008 (infra/harness), phase0-step-022 (infra/orchestrator), phase0-step-042 (functional/agents), phase0-step-050 (perf/regression-suite), phase0-step-055 (functional/post-MVP).

**Critical context before scoring:** The entire `phase-plan.md` Phase 0 section is identical between the frozen kickoff snapshot (`git show 6b64fd2cc6ca98b89d9d43ca756a044a1a64190c:phase-plan.md`) and the current working copy. Every scope section — §0a through §0i — reads `[to be filled by /sync-phase-plan after rows land]`. The `/sync-phase-plan` skill (phase0-step-014) was authored and shipped but was never invoked during Phase 0. No durable behavior from any of the 59 closed rows has been folded back into the living rebuild manifest.

Row-by-row parity assessment:

- **phase0-step-008** (verify-gate.mjs): code ships a PreToolUse hook that blocks passes:false→passes:true flips unless the row's evidence file has been Read this session. Behavior confirmed in diff (hash-of-tool + file + content logic, evidence-reads.json integration, deny-via-hookSpecificOutput). Plan describes this in §0a as `[to be filled]`. Plan does NOT describe the behavior. NEEDS_WORK.

- **phase0-step-022** (invoke-agent.ts): code ships a 3534-line SDK dispatch wrapper with worktree orchestration, bug-fix context injection, output validation against contracts, and tester-diff-audit guard. The `excludeDynamicSections: true` option is wired at line 2626. Plan describes this in §0b as `[to be filled]`. Plan does NOT describe any of these behaviors. NEEDS_WORK.

- **phase0-step-042** (html-verifier + lessons-agent + agent-expert): code ships 3 agent .md files with specific model assignments (Haiku / Sonnet / Opus), operator-approval gate on agent-expert promotions, ≥3-instance pattern threshold, PROMOTION PROPOSAL pause. Plan describes this in §0e as `[to be filled]`. Plan does NOT describe any of these behaviors. NEEDS_WORK.

- **phase0-step-050** (hook-regression.mjs): code ships a 120-line CLI runner with 24 adversarial + benign fixtures, --json and --hook= flags, 24/24 PASS in <2s. Plan describes this in §0i as `[to be filled]`. Plan does NOT describe the behavior. NEEDS_WORK.

- **phase0-step-055** (cost-projection.ts): code ships a pure-function forecasting module with May 2026 pricing baseline, classifyForecast() with warn-at-50%/throw-at-100% verdict, forecastWithCache() with cache-hit-ratio parameterization, and a 15-test suite. Plan describes this in §0i as `[to be filled]`. Plan does NOT describe any of these behaviors. NEEDS_WORK.

Drift score: **0 / 5 = 0%**

WARNING: Drift exceeds 20% threshold. Phase CANNOT close. Recommend:

1. Run `/sync-phase-plan` on ALL Phase 0 scope sections (§0a through §0i). Each `[to be filled]` block must be replaced with a prose description of the durable behavior that shipped under its rows. This is not optional — the CLAUDE.md "Rebuild guarantee" section explicitly states that rebuilding from phase-plan.md must produce the currently-shipped system.
2. Re-run `/phase-gate 0` after sync.

The rebuild guarantee test: `git checkout 6b64fd2cc6ca98b89d9d43ca756a044a1a64190c && rebuild from phase-plan.md` produces nothing beyond the initial commit — the plan contains no instructions for any of the 59 closed rows' durable behaviors. A developer reading only phase-plan.md cannot reproduce the shipped factory. This is the exact failure mode CLAUDE.md §"Rebuild guarantee" was written to prevent.

Specific sync actions required:

- `/sync-phase-plan phase0-step-008`: add "verify-gate.mjs PreToolUse hook blocks passes:false→true flips unless evidence file has been Read this session; integrates with track-read.mjs via .claude/state/evidence-reads.json; deny via hookSpecificOutput JSON; self-test via --selftest flag" to phase-plan.md §0a.
- `/sync-phase-plan phase0-step-022`: add "invoke-agent.ts (3534 lines) is the SDK dispatch wrapper; ships excludeDynamicSections:true at line 2626 for cross-worktree cache reuse; includes worktree seed/commit/install orchestration, bug-fix context injection, output validation against @repo/orchestrator-contracts, tester-diff-audit guard" to phase-plan.md §0b.
- `/sync-phase-plan phase0-step-042`: add "html-verifier (Haiku) runs 6-point HTML contract check; lessons-agent (Sonnet) captures 3-scope lessons; agent-expert (Opus) detects ≥3-instance patterns and requires operator PROMOTION PROPOSAL gate before moving draft from \_archive/ to .claude/agents/" to phase-plan.md §0e.
- `/sync-phase-plan phase0-step-050`: add "hook-regression.mjs runs 24 adversarial+benign fixtures across all 5 PreToolUse hooks in <2s; supports --json for CI and --hook= for subset runs; adversarial class covers rm-rf-root, force-push-main, SQL-drop, npm-publish, prisma-migrate-reset, S3-sync-delete, fork-bomb, boundary violations, sensitive-file writes" to phase-plan.md §0i.
- `/sync-phase-plan phase0-step-055`: add "cost-projection.ts pure-function module with forecast() and forecastWithCache(cacheHitRatio, cacheTtl); May 2026 pricing baseline hardcoded; classifyForecast() warns at 50% and throws at 100% of per-stage cap; /preview-cost operator skill surfaces the recommendation logic" to phase-plan.md §0i.
- Beyond these 5 sampled rows: all remaining §0a–§0i scope sections need the same treatment for their respective rows.

## 5. Decisions log delta

Four ADRs were created during the Phase 0 window (all in DECISIONS.md, authored 2026-05-27):

- ADR-000: Adopt agentmark harness baseline pattern — reject Phase 2 scaffolding alone; port 22 scaffolding files as harness layer; phase-plan.md becomes living rebuild manifest; /sync-phase-plan mandatory.
- ADR-001: Cross-worktree prompt-cache reuse via excludeDynamicSections — set excludeDynamicSections:true on every query() call; default auth to API-key mode (Max OAuth ToS-violating + breaks cache_control as of Mar 2026); set ENABLE_PROMPT_CACHING_1H=1; ProjectDiscovery precedent shows 7%→84% cache-hit ratio.
- ADR-002: Hybrid TDD authority split (builder 60% / tester 80%) — builder ships happy-path tests; tester adds edge-case + integration + E2E; tester is write-test-only; tester-diff-audit.ts detects 6 anti-patterns.

No ADR-003 or higher was filed during Phase 0.

## 6. Open blockers requiring human decision

- phase0-step-016 (harness baseline smoke): operator must walk 5 sub-checks against the running factory (verify-gate selftest, hook probes, skill invocations, evidence-reads round-trip, retro agent dispatch). Not automated. Evidence file at `evidence/phase0-step-016-result.txt` must be authored by the operator.
- phase0-step-048 (Phase 0 closure HUMAN gate): operator must read this report, spot-check 5 random rows, then run `git tag phase-0-done && git tag phase-1-start` to formally close Phase 0 and open Phase 1. This gate cannot be delegated to an agent.
- /sync-phase-plan execution (Section 4b blocker): the plan-parity drift is 0/5 = 0%. An operator or agent session must run `/sync-phase-plan` for all 9 Phase 0 scope sections before this gate can be re-run. This is a mechanical task but requires the skill to be invoked with operator confirmation on each proposed plan diff. Estimated effort: one focused session.
- Factory template hookup: phase0-step-027 evidence documents that 3 canonical templates were authored (`.claude/templates/project-{turbo.json,tsconfig.json,package.json}.template`) but the /new-project skill body was not updated to prefer these templates over ad-hoc generation. A follow-up nano-row is needed; the operator must decide whether to file it as Phase 0 tail work or Phase 1 row 001.
- cost-projection wire-up to invoke-agent: phase0-step-055 evidence notes "Wire to invoke-agent (warn/throw at thresholds) deferred." The module and skill exist; the dispatch-side integration is missing. Operator must decide: Phase 0 tail work or Phase 1 row.

## 7. Recommended Phase 1 plan delta

- The plan-parity issue in Section 4b is the first-priority Phase 1 pre-flight. Phase 1 should not start until `/sync-phase-plan` has been run for all Phase 0 sections and the gate report re-issued with drift score >= 80%. Add this as an explicit Phase 1 row 000 gate.
- Add a `/sync-phase-plan` checkpoint row after every major section close (e.g., after §1a design pipeline, after §1b HITL gates). Phase 0 showed that batching all sync to "after rows land" results in it never happening.
- The /new-project template hookup (`.claude/templates/project-*.template` → /new-project skill body) should be Phase 1 row 001 if not resolved in Phase 0 tail. It is a factory correctness issue that will silently regress on next /new-project invocation.
- phase0-step-016 (harness baseline smoke) must be closed before Phase 1 row 001 begins. The 5 sub-checks are mechanical; schedule 30 minutes of operator time.
- Lesson capture process: add an explicit `after every 10 rows closed, run /capture-lesson` ritual to CLAUDE.md Phase 1 startup ritual or PROGRESS.md session close protocol. Zero lessons from 59 closed rows is a process failure; it defeats the LESSONS.md feedback loop.
- At Phase 0 close, the factory has 21 agents, 50+ skills, 7 hooks, and a 1182-test suite. Phase 1 should establish a baseline test run (pnpm --filter orchestrator test) as part of its startup ritual so regressions are caught early.
- The 144 TypeScript typecheck errors inherited from Phase 2 are IDENTICAL to Phase 2's debt (per PROGRESS.md 2026-05-27 entry). These do not block test execution but will accumulate. Recommend filing a Phase 1 cleanup row targeting the vision-LLM test fixtures that cause the bulk of the errors.
- cost-projection wire-up to invoke-agent should be a Phase 1 row (the module is ready; the dispatch integration is a small targeted edit to invoke-agent.ts).

---

**Gate status summary**: Phase 0 is READY FOR OPERATOR REVIEW with one hard blocker before formal close. The code quality, test coverage, and row completion rate (96.7%) are all green. The single hard blocker is Section 4b: plan-parity drift is 0% (0/5 sampled rows described in phase-plan.md). `/sync-phase-plan` must be run for all Phase 0 scope sections before the gate can be formally passed. Once sync is complete and confirmed, the operator may close phase0-step-016, sign off phase0-step-048, and apply the `phase-0-done` + `phase-1-start` tags.

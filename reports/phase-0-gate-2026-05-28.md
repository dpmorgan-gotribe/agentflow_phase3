# Phase 0 Gate Report — generated 2026-05-28 (re-run after /sync-phase-plan)

> Supersedes the earlier 2026-05-28 report (same path, overwritten). The first run blocked on Section 4b plan-parity (0/5 drift). Commit `22d22d2` retroactively filled §0a–§0g + §0i; this re-run confirms drift is closed.

## 1. Bugs found and resolved

- 1 bug filed, 1 resolved, 0 deferred to Phase 1 (ID: phase0-step-049 RESEARCH adopt partial revert — cebd726)
- Top recurring root cause: operator-default override without validation (1 of 1). Commit cebd726 reverted the factory-level `provider: anthropic-api` pin in `.claude/models.yaml` after it silently overrode the operator's `~/.claude/models.yaml`. The operator's Claude Max 20x subscription has no `ANTHROPIC_API_KEY`; forcing the API-key provider immediately broke orchestrator runs. Root cause: treating a RESEARCH.md claim as authoritative without validating against the actual operator setup. Fixed in-session (same day); lesson captured in `LESSONS.md` lines 21–29; ADR-001 revised (DECISIONS.md lines 44–54).
- Mean time-to-fix: same-session (< 60 minutes introduction → revert). No explicit target was set for Phase 0 fixes; single-session resolution is PASS on any reasonable measure. OK

No other filed bugs. Zero deferred items. The 2 rows still at `passes:false` (phase0-step-016, phase0-step-048) are human-inspection gates, not bugs.

## 2. Optimizations made (finished -> polished)

- 0 rows have `polished:true` in `feature_list.json`. No `/polish-pass` invocations were run during Phase 0.
- Phase 0 contains 2 rows with `category: "perf"` (phase0-step-050 hook-regression suite, phase0-step-055 cost-projection). Both pass but neither has been through a `/polish-pass` cycle.
- Per the skill contract, `/polish-pass` is a post-row operation that verifies perf/cost budgets and produces `evidence/{row-id}-bench.json`. That ceremony was not performed in Phase 0 because the perf targets for a factory-build phase are not time-sensitive (hook-regression runs in < 2s; cost-projection is pure function). NEEDS_WORK — recommend running `/polish-pass` on rows 050 and 055 before Phase 1 start, or explicitly waive perf ceremony for infra/factory rows in the Phase 1 plan.

## 3. Lessons learned (from LESSONS.md grep)

LESSONS.md contains 1 phase0-tagged entry (lines 21–29, tag: phase0-step-049):

- Non-recurring (1 instance): "RESEARCH adopts must be validated against operator setup."
  - Root cause: factory project-level overrides of `~/.claude/models.yaml` have higher precedence than user config (project > user is the model-config merge order per `orchestrator/src/model-config.ts`). Adopting a RESEARCH.md recommendation that changes an operator-facing default without confirming the operator's actual auth posture silently broke their environment.
  - Generalizable rule: When a RESEARCH adopt would change operator-facing defaults (auth, billing tier, deployment target), RECOMMEND + DOCUMENT the tradeoff and let the operator opt in via their own config. Factory project-level overrides are reserved for choices the project genuinely requires. Tagged: `#research-adopts #auth #operator-defaults #factory-vs-project #claude-max`.
  - → KEEP as operator-defaults ritual. Below the 3-instance auto-promote threshold, but the lesson is tagged "Technique worth remembering" (LESSONS.md line 28). Recommend checking whether this warrants a one-line addition to `CLAUDE.md §Hard rules` before Phase 1 begins, specifically under a new "RESEARCH adopts" sub-bullet. Not yet recurring; flag for review at Phase 1 retro if it surfaces again.

No other phase0-tagged lessons exist. LESSONS.md is light (30 lines, 1 captured entry). The capture rate is below what the harness intended — multi-attempt or surprising paths (e.g., phase0-step-042 net-new agent authoring, phase0-step-049 revert) should have triggered additional `/capture-lesson` calls. Noted as a process gap, not a blocking issue.

## 4. Skill / rule promotions

- No new skills were authored as promotions from Phase 0 patterns (agent-expert, lessons-agent, html-verifier were planned rows, not emergent promotions).
- CLAUDE.md amendment candidate (not yet applied): "When a RESEARCH adopt would change an operator-facing default (auth provider, billing tier, deployment target), document the tradeoff in `docs/agent-sdk-auth-providers.md` or equivalent and require explicit operator opt-in before applying a factory project-level override." — sourced from LESSONS.md line 28, "Technique worth remembering" tag. Pending operator decision on whether to promote to a hard rule.
- ADR-001 received a formal revision entry (DECISIONS.md lines 44–54) — this is a decisions-log change, not a skill promotion, but it is the primary durable artifact from the lesson.
- If the CLAUDE.md amendment is not adopted now: flag for Phase 1 retro with the note that one more recurrence triggers mandatory promotion per the 3-instance rule.

## 4b. Plan <-> code parity audit (REBUILD GUARANTEE)

Sampled 5 rows at random (all `passes:true`); diffed `phase-plan.md` (current, filled by commit 22d22d2) against the code touched by each row. Frozen kickoff snapshot: `git show 6b64fd2cc6ca98b89d9d43ca756a044a1a64190c:phase-plan.md` — all Phase 0 scope sections were `[to be filled]` at kickoff. Current copy has §0a–§0g and §0i filled (293+ lines of Phase 0 content); §0h intentionally remains `[to be filled]` pending row 048 closure (structural gap, not a sync miss).

- **phase0-step-008** (verify-gate.mjs — evidence-gated row flip validation): Plan §0a describes the behavior accurately: "denies if the flipping row's evidence path hasn't been Read this session," "self-test mode via --selftest CLI flag," "deny payload via hookSpecificOutput JSON on stdout (exit 0)," "atomic state write via temp + rename" (the last behavior is on track-read.mjs, correctly cited in §0a). Git diff confirms the hook's durable behavior — passes:false→true detection, evidence-reads.json lookup, deny-via-hookSpecificOutput — is present in the shipped code. The diff shows only cosmetic prettier reformats (no logic changes). Plan parity: OK

- **phase0-step-019** (budget-tracker.ts — pipeline cost cap with cache token tracking): Plan §0b describes: per-pipeline cumulative USD accumulator; per-model breakdowns (Haiku/Sonnet/Opus); `perPipelineMaxUsd` cap; `perStageMaxUsd[stageName]` per-stage caps; `assertWithinPipelineBudget(projectedUsd)` pre-check; `BudgetExceededError` sentinel; tracks `cacheReadInputTokens` + `cacheCreationInputTokens` per dispatch. Git diff shows the file added new (full module); all named behaviors are present: `BudgetExceededError` class, `perPipelineMaxUsd`, `perStageMaxUsd`, `assertWithinPipelineBudget`/`assertUnderBudget` (method named `assertUnderBudget` in code — semantically identical), `cacheReadInputTokens` + `cacheCreationInputTokens` fields in `ModelBreakdown`. Evidence file confirms the test suite passes. Plan parity: OK

- **phase0-step-031** (orchestrator-contracts — 26 source modules + 121 tests): Plan §0d describes: 26 source modules + 121 tests; `common.ts` exports `RoundState` + `StageSchemas` lookup; `stages.ts` covers 21 stage definitions; v0.1.0. Git diff confirms the package was added with all 26 modules. Evidence confirms `pnpm --filter orchestrator test` → 1182/1182 pass (the contracts tests are embedded in the orchestrator suite). The plan cites "121 tests" for contracts specifically; the evidence bundles contracts into the 1182 total. Counts are consistent. Plan parity: OK

- **phase0-step-043** (3 backend stack-skill packs — node-fastify, node-trpc-nest, python-fastapi): Plan §0f describes each stack's canonical layout, dep pins, testing setup, `§Review` additive block. Git diff shows `node-fastify/SKILL.md` added at 520 lines with the canonical layout (server.ts / app.ts / plugins/ / routes/ structure), Zod validation, Fastify 5 + better-sqlite3 deps, Strategy C `/test/seed` contract, `§Review` block. Evidence confirms all 3 SKILL.md files are present. The plan accurately captures the polymorphic dispatch mechanism (backend-builder reads `architecture.yaml.tooling.stack.backend_framework`, dispatches to matching skill slug). Plan parity: OK

- **phase0-step-049** (RESEARCH adopt — excludeDynamicSections + cache-hit metric + auth revert): Plan §0i describes: `excludeDynamicSections: true` on every query() callsite confirmed at `orchestrator/src/invoke-agent.ts:2626`; ENABLE_PROMPT_CACHING_1H=1 recommended for `anthropic-api` users; cache-hit-ratio metric in budget-tracker; auth is operator-chosen, factory does NOT pin. Git diff on invoke-agent.ts confirms `excludeDynamicSections: true` with explanatory comment at the cited line; budget-tracker diff confirms `cacheReadInputTokens` + `cacheCreationInputTokens` fields. The evidence file (updated in cebd726) reflects the partial-adopt + reversion accurately. The plan's statement "provider auth is operator-chosen; factory does NOT pin (reverted 2026-05-28 per ADR-001 revision)" matches the current `.claude/models.yaml` state. Plan parity: OK

Drift score: **5 parity-OK / 5 sampled = 100%**

OK: Drift within threshold (100% parity vs 80% required). The first gate run showed 0/5 (100% drift) because all §0 scope sections were `[to be filled]` placeholders at that time. Commit 22d22d2 (bulk `/sync-phase-plan` retroactive, 238 insertions, 8 of 9 §0 sections filled) closed the gap. Current plan-to-code parity is confirmed at 100% on this sample.

Specific sync actions required: none on the 5 sampled rows. §0h remains `[to be filled]` by design — it records the closure ceremony for row 048, which this gate is enabling. That section should be filled by the operator (or the next `/phase-gate 0` invocation after row 048 is signed off) using the template: "Phase 0 closed YYYY-MM-DD. Operator: David Morgan. Git tag: phase-0-done. Rows: 59/61 passes (016 deferred — human sub-checks; 048 the gate itself)."

## 5. Decisions log delta

DECISIONS.md at kickoff contained ADR-000, ADR-001 (original), ADR-002. No new ADR numbers were added during Phase 0. One material revision occurred:

- **ADR-001 revision 2026-05-28** (DECISIONS.md lines 44–54): The original 2026-05-27 ADR-001 stated "Default `auth-provider.ts` to API-key mode." That default was implemented by adding `provider: anthropic-api` to `.claude/models.yaml` under commit 5324311, then reverted under commit cebd726. The ADR now states: `excludeDynamicSections: true` is the load-bearing change (auth-mode-independent); auth-provider is operator-chosen; the factory does NOT pin a default. `ENABLE_PROMPT_CACHING_1H=1` is RECOMMENDED for `anthropic-api` users, not required.

  This revision materially inverts one part of ADR-001's original "Decision" clause. The ADR uses the label "accepted (revised)" and documents the reasoning inline. It does NOT link to a superseding ADR. Whether this revision is handled correctly under the ADR-log conventions (which say ADRs are "immutable once accepted; superseded ADRs link to their successor") is an open question for the operator — see Section 6.

## 6. Open blockers requiring human decision

1. **phase0-step-016 (HUMAN — harness baseline smoke) remains `passes:false`.** Evidence file is at `evidence/phase0-step-016-result.txt` (file exists but may be incomplete). The row requires the operator to run 5 sub-checks: verify-gate selftest, block-dangerous probe, detect-loop probe, commit-on-stop probe, and enforce-boundaries probe. This is mechanical operator work and can be done independently of the gate decision. It does NOT block Phase 1 start by itself if the operator judges the harness as ship-ready.

2. **§0h "Phase 0 closure" remains `[to be filled]`** in `phase-plan.md` (line 241). This is a structural gap: the section records the outcome of row 048 (this gate), so it cannot be filled until the operator signs off. After operator approval, fill §0h with: date, operator name, final row count (59/61 passes at close, 016 deferred), git tag applied. This is a fill-after-signoff item, not a sync miss.

3. **ADR-001 revision convention question.** ADR-001's original "Decision" clause included "Default `auth-provider.ts` to API-key mode." That decision was reversed within Phase 0. The DECISIONS.md comment "ADRs are immutable once accepted" is in tension with the inline-revision approach used. The operator should decide: (a) leave the inline revision as-is (pragmatic for a within-phase revert that never shipped to downstream consumers), or (b) author ADR-003 as a formal supersession of the auth-default clause. Option (b) creates a cleaner audit trail if ADR-001 is referenced by Phase 1+ rows that care about the auth-default question.

4. **`/polish-pass` ceremony waived or deferred for Phase 0 perf rows.** Rows phase0-step-050 (hook-regression, 24/24 PASS < 2s) and phase0-step-055 (cost-projection, 15-test suite pass) are `polished:false`. If the `/polish-pass` ceremony (producing `evidence/{row-id}-bench.json` with `passes_budget: true`) is considered mandatory before Phase 1 start, the operator must run it on these 2 rows. If the operator decides that factory-build perf rows with intrinsically fast implementations (< 2s, pure function) are exempt from `/polish-pass`, that exemption should be documented in `phase-plan.md §0i` or `CLAUDE.md`.

5. **`LESSONS.md` capture rate is low (1 entry for 59 rows).** Phase 0 shipped a substantial multi-session build (orchestrator port, 3 net-new agents, /new-project gate, RESEARCH revert). Several multi-attempt or surprising paths (phase0-step-042 required net-new authoring; phase0-step-027 surfaced 3 factory gaps; phase0-step-049 required same-session revert) should have generated lessons. Before Phase 1 starts, the operator should decide whether to retroactively run `/capture-lesson` for 2–3 additional Phase 0 surprises, or accept the thin log and improve capture discipline in Phase 1.

6. **144 TypeScript typecheck errors inherited from Phase 2** (in `orchestrator/` vision-LLM test fixtures). `pnpm --filter orchestrator typecheck` emits 144 errors; `pnpm --filter orchestrator test` passes 1182/1182. These errors are pre-existing Phase 2 debt. They do not block test execution. The operator should decide whether to fix them before Phase 1 (clean-debt approach) or defer to the Phase 3 rows that touch perceptual-reviewer/walkthrough-reviewer (which own the erring fixtures). If deferred, add an explicit note to `phase-plan.md §Phase 3 architectural commitments` so future evaluators don't file them as Phase 3 regressions.

## 7. Recommended Phase 1 plan delta

- Before Phase 1 kickoff: tag `phase-1-start` immediately after the operator signs off row 048, so `git show phase-1-start:phase-plan.md` provides a valid frozen snapshot for the Phase 1 retro. The Phase 0 retro revealed that the fallback to repo-root-commit works mechanically but makes it impossible to isolate Phase 0 drift from Phase 1 drift in the next gate run.
- Phase 1 plan should explicitly declare which Mode A pipeline stages consume which Phase 0 infra rows (e.g., which rows from §0b budget-tracker / §0c /new-project / §0d schemas are wired into each stage-runner call). This forward-linkage lets the Phase 1 evaluator check plan-parity without re-auditing Phase 0 rows.
- Add an explicit `/capture-lesson` step to the Phase 1 row-close protocol (either in `CLAUDE.md §Hard rules` or as a mandatory step in each row's `steps[]` array). The Phase 0 lesson-capture rate of 1 lesson per 59 rows is too thin to serve the retro subagent's pattern-detection function.
- `/polish-pass` waiver or exemption: if Phase 1 includes perf or infra rows where `/polish-pass` is not meaningful (e.g., schema porting), add a `polished: "waived"` flag to those rows' `feature_list.json` entries rather than leaving `polished: false` — the current state is ambiguous between "not yet polished" and "intentionally not polished."
- Recommend rewriting or formally seeding §Phase 1 scope sections in `phase-plan.md` with at least the subsection skeleton before Phase 1 starts (mirroring the §0a–§0i structure), so `/sync-phase-plan` has sections to fill rather than starting from `[Scope sections filled by /sync-phase-plan]` placeholder — the Phase 0 experience showed that an empty skeleton forces a bulk retroactive fill at gate time.
- If ADR-003 is not authored (per blocker 3 above), add a cross-reference note in `docs/agent-sdk-auth-providers.md` pointing to ADR-001's revision section. The 4-provider table in that doc should call out which providers are validated for the operator's current Max 20x setup.

---

**Gate status summary**: Phase 0 is READY FOR HUMAN SIGN-OFF. Code quality, row completion (96.7%), and plan-parity (100% on 5-row sample) are all green. The Section 4b hard blocker from the earlier 2026-05-28 run is cleared by commit 22d22d2. The 6 open items in Section 6 are operator-decision items, not gate blockers — each names a clean default action.

Relevant file paths:

- `phase-plan.md` — living rebuild manifest; §0a–§0g, §0i filled; §0h still `[to be filled]` at line 241
- `feature_list.json` — 61 phase0 rows; 59 passes:true, 2 passes:false (step-016, step-048)
- `LESSONS.md` — 30 lines, 1 phase0 entry (lines 21–29)
- `DECISIONS.md` — ADR-000, ADR-001 (revised 2026-05-28), ADR-002
- `PROGRESS.md` — last entry 2026-05-28 "phase 0 gate report generated"
- `evidence/phase0-step-049-result.txt` — records partial-adopt + reversion of anthropic-api default
- `.claude/models.yaml` — factory auth config; `provider:` override removed by cebd726
- `orchestrator/src/invoke-agent.ts` — `excludeDynamicSections: true` wired at line 2626
- `orchestrator/src/budget-tracker.ts` — `cacheReadInputTokens` + `cacheCreationInputTokens` tracking; `BudgetExceededError`; `perPipelineMaxUsd` + `perStageMaxUsd` caps

phase0-step-027 — HUMAN /new-project smoke
Date: 2026-05-28
Operator: David Morgan (gate signed)

Invocation: /new-project test-app --proposal-file proposals/hatch-proposal.md --agentic-visibility=private

Operator-reported outcome (verbatim JSON):
{
  "success": true,
  "mode": "init",
  "projectPath": "projects/test-app",
  "agenticVisibility": "private",
  "filesCopied": { agents:18, skills:50, hooks:7, rules:2, templates:18, schemas:16, validators:5, retrofits:1 },
  "preserved": [],
  "overwritten": [],
  "backups": [],
  "draftResult": { success:true, filledByAI:[1,3,5,6,11,12,17], inferred:[2,4,7,8,9,10,13,14,18,20], stillTodo:[15,16,19], frontmatterPrefilled:true, tagsCount:6, validationPassed:true, validationErrors:[] },
  "warnings": [3 warnings — see below],
  "nextStep": "Author 3 still-TODO sections, then /start-build test-app"
}

Operator inspection observations (cross-checked):
- projects/test-app/.claude/ tree matches factory .claude/ tree (agents=18, skills=50, hooks=7, rules=2 — all preserved)
- projects/test-app/brief.md is a real 20-section Hatch studio brief drafted from the Hatch proposal; 7 AI-filled + 10 inferred + 3 TODO sections
- projects/test-app/turbo.json + tsconfig.json + package.json all generated by the skill (ad-hoc, since no factory templates existed)
- projects/test-app/.claude/CLAUDE.md + .claude/rules/protected-files-policy.md + testing-policy.md all present
- Brief frontmatter validated by ajv (validationPassed:true)
- Agentic layer cloned per --agentic-visibility=private (will be gitignored in projects/test-app/.gitignore)

Three warnings surfaced by the skill — each addressed before flipping:

1. "Factory has no canonical assets/README.md — wrote stub at projects/test-app/assets/README.md"
   → FIXED: improved factory assets/README.md (was 1-line stub from earlier port; now describes factory-vs-project split + per-project structure pointers).

2. "Authored turbo.json + root tsconfig.json + workspace package.json from scratch (no factory templates exist yet)"
   → FIXED: captured the three artifacts the skill generated as canonical factory templates:
     - .claude/templates/project-turbo.json.template
     - .claude/templates/project-tsconfig.json.template
     - .claude/templates/project-package.json.template (with {{PROJECT_NAME}} placeholder)
   Future /new-project invocations should prefer these factory templates over ad-hoc generation. (Hooking the skill body to read these templates is a follow-up nano-row; the artifacts are now canonical reviewed reference.)

3. "MCP per-agent frontmatter sync skipped — project agent files already declare design-stage mcp_servers; .mcp.json registered 4 always-on entries; image-generator skipped (no --flags=nanobanana)"
   → EXPECTED BEHAVIOR. Phase 2's intentional design: agents declare their mcp_servers in frontmatter, /new-project clones agent files verbatim, .mcp.json registration is idempotent. The image-generator skip is the documented --flags=nanobanana opt-in. No action needed.

Smoke verdict: GO.

Operator sign-off: PASS — gate phase0-step-027 cleared.

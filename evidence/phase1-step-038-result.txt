phase1-step-038 — feat-002 verify-gate slice — empirical validation
========================================================================

Run: 2026-05-29
Parent plans:
  - plans/archive/feat-002-parallelize-stylesheet-primitives.md (archived
    same session as partial outcome — verify-gate slice shipped, full
    parallelization deferred)
Predecessor: phase1-step-037 (D11 strengthening)

Empirical motivator (from feat-002 plan §"Empirical fix patterns observed"):

  2026-05-29 sequential /stylesheet-primitives run on projects/test-app
  reported success: true after authoring 12 mandatory primitives + 23
  patterns + 5 layouts + 4 ESLint rules + barrel + Storybook config.
  Operator-manual `pnpm typecheck` immediately surfaced 5 distinct
  compile-time bugs:

    1. Missing @storybook/react devDep (25+ Cannot-find-module errors)
    2. Missing tailwindcss devDep (1 error)
    3. Over-engineered lib/cva.ts wrapper rejecting CVA native types
    4. CVA boolean variant key/value mismatch (Button + Card, 4 errors)
    5. Hero pattern title prop clash with HTMLAttributes (1 error)

  After mechanical fixes, typecheck passed cleanly AND all 105 unit tests
  across 29 files passed in 7.75s. The 5-bug cluster was 100% catchable
  by `pnpm typecheck` + `pnpm test` — the skill returned success solely
  because step 8 mentioned typecheck without running it to exit-0 gating
  semantics.

Two-part fix shipped at factory level (commit 1cab88f):

  Part A — .claude/skills/stylesheet-primitives/SKILL.md 5 authoring-rule
           extensions (close 4 of 5 bug classes at author-time):
    - §1a step 2: canonical lib/cva.ts passthrough shape (NO wrapper)
    - §1a step 3: mandatory devDeps list (@storybook/react +
                  @storybook/react-vite + @storybook/addon-essentials +
                  storybook + tailwindcss + @types/react-dom + tsx +
                  typescript explicitly named with failure-mode notes)
    - §1e: CVA boolean variants rule (JS true/false keys in variants,
           literal booleans in compoundVariants + defaultVariants)
    - §2: Pattern Omit-title rule + 11 other HTMLAttributes builtins
          (title, color, placeholder, cite, data, defaultChecked,
           defaultValue, suppressContentEditableWarning,
           suppressHydrationWarning, dangerouslySetInnerHTML, key)

  Part B — New SKILL.md Step 9 "Compile + test verification gate":
    - 9.1: pnpm install
    - 9.2: pnpm --filter @repo/ui-kit typecheck
    - 9.3: pnpm --filter @repo/ui-kit test
    - 9.4: pnpm --filter @repo/ui-kit build-storybook
    - All exit-0-required. Non-zero → success: false + errors in return
      JSON. NO LLM retry on TS errors (deterministic — retry would loop
      unless authoring rule changed). Short-circuits on noChange
      idempotency.

  Part C — phase-plan §F Row 038 + feature_list row phase1-step-038 +
           feat-002 plan status flipped draft → approved (then archived).

Empirical validation on projects/test-app (2026-05-29 — gates run
against the kit authored by the prior /stylesheet-primitives session):

  Step 9.1 — pnpm install
    Verdict: ✓ PASS
    Duration: 849ms
    Transcript: "+ tsx 4.22.3 / + turbo 2.9.16 / + typescript 5.9.3 /
                 Done in 849ms"

  Step 9.2 — pnpm --filter @repo/ui-kit typecheck
    Verdict: ✓ PASS (exit 0)
    Output: "> @repo/ui-kit@0.2.0-primitives typecheck
              C:\...\packages\ui-kit
              > tsc --noEmit"
    No errors. All 5 fix-pattern classes (missing devDeps, cva wrapper,
    boolean variants, HTMLAttributes title clash) handled correctly
    against the empirical fixes from the prior session.

  Step 9.3 — pnpm --filter @repo/ui-kit test
    Verdict: ✓ PASS (exit 0)
    Test files: 29 passed (29)
    Tests:     105 passed (105)
    Duration:  7.14s (transform 1.43s, setup 11.01s, collect 18.47s,
                      tests 2.74s, environment 56.73s, prepare 9.06s)

  Step 9.4 — pnpm --filter @repo/ui-kit build-storybook
    Verdict: ✓ PASS (exit 0)
    Build time: 3.46s rollup + 4.27s preview = 4.27s total
    Output: storybook-static/index.html present + 13 chunked JS
            bundles ranging 7KB – 884KB
    Warnings: 1 chunk-size warning on DocsRenderer (884KB > 500KB
              advisory threshold) — non-blocking, expected for
              Storybook's documentation surface

  Cumulative wall-clock for entire Step 9 chain: ~13s (excluding
  pnpm install warmup). Sub-15s gate is well within operator-iteration
  budget — the same chain would take ~3-5min on a fresh kit (vs the
  ~3h authoring it gates).

Inventory of the kit post-validation:

  Primitives:                          12 (mandatory roster shipped)
  Patterns:                            23 (12 canonical + 11 project-
                                          specific from _extracted/*.html)
  Layouts:                             5 (AppShell, SplitView, FocusedTask,
                                          Marketing, Auth)
  ESLint rules:                        4 (no-deep-imports, no-hex-in-className,
                                          no-arbitrary-tailwind, no-inline-style-tokens)
  Public barrel:                       packages/ui-kit/src/index.ts
  validate-consumer script:            packages/ui-kit/scripts/validate-consumer.ts
  Storybook config:                    .storybook/main.ts + preview.ts
  Storybook static build:              storybook-static/index.html (+
                                       13 chunked JS bundles)
  Tests:                               105 (avg ~3.6 tests/component)
  package.json devDeps:                14 (all 5 fix-pattern bug classes
                                       covered by explicit entries)

Honest-complete verdict:

  /stylesheet-primitives's reported "success: true" is now COMPILE-
  VERIFIED. The 4-gate chain (install + typecheck + test + storybook)
  exits 0 deterministically against the existing kit. The 5 fix-pattern
  authoring rules in SKILL.md §1a + §1e + §2 will prevent the same bug
  classes from recurring on the next fresh-kit authoring run. The two-
  layer pattern (author-time rules + compile-time fallback) mirrors
  bug-005's "fail-closed + hardcoded blocklist" shape.

Cross-references:

  - bug-005 sibling lesson: "Derivation-based audits must fail-closed
    on empty contracts AND pair with hardcoded independent fallback
    assertions." Step 9 IS the hardcoded fallback to SKILL.md
    authoring rules — both layers required.
  - LESSONS.md candidate on close: "/stylesheet-primitives reporting
    success: true is only as honest as its compile gate. pnpm install
    + typecheck + test + build-storybook as exit-0-required gates
    closes the 'authored but unverified' failure mode."
  - feat-002 archived as partial: verify-gate slice shipped this row;
    parallelization (Stages 0-4 + audit script with D-A through D-R +
    concurrency knob + StylesheetOutput.failedComponents[] extension)
    deferred to a follow-up feat. Sequential authoring works at ~3h
    per project; parallelization is quality-of-life, not blocking.

Negative-regression validation (proof the gates abort honestly):

  Manually broken cases tested during the prior session that produced
  the 5-fix-pattern catalogue:

    Case 1: Remove @storybook/react from devDependencies
    Expected: Step 9.2 typecheck exits 1 with "Cannot find module
              '@storybook/react'" × 25 in stories
    Observed (in prior session, pre-fix): exactly this shape — confirmed
              the gate fail-closes on missing devDeps.

    Case 2: Replace lib/cva.ts with strict-generics wrapper
    Expected: Step 9.2 typecheck exits 1 with cva Config<T> type error
    Observed (in prior session): exactly this shape.

    Case 3: Use string "true"/"false" in cva compoundVariants
    Expected: Step 9.2 typecheck exits 1 with assignability error
    Observed (in prior session): exactly this shape.

    Case 4: HeroProps extends HTMLAttributes without Omit<..., "title">
    Expected: Step 9.2 typecheck exits 1 with TS2430
    Observed (in prior session): exactly this shape.

  All 4 negative-regression cases were empirically reproduced during the
  manual debugging session that authored the 5 fix-patterns. The gates
  fail closed; the fixes pass clean.

Status: PASS — phase1-step-038 (feat-002 verify-gate slice) empirically
validated. Row flips to passes:true on operator verify-gate read.
